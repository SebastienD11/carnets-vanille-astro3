---
const { node } = Astro.props
import Card from '../components/post/card.astro'
import { type Post } from '../api/getIndexPosts'
---

<h1>{node.name} - Archive</h1>

{
  node?.posts?.nodes.map((post: Post) => {
    return <Card post={post} />
  })
}
