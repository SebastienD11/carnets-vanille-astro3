---
import { getRelativeLocaleUrl } from 'astro:i18n'
import { locales } from '../../i18n/ui'
const currentLang = Astro.currentLocale as string
---

<ul class="flex items-center justify-center gap-4">
  {
    locales.map((lang) => (
      <li>
        {lang === currentLang ? (
          <strong class="text-zink-600 dark:text-zink-300 text-sm font-bold uppercase">
            {lang}
          </strong>
        ) : (
          <a
            href={getRelativeLocaleUrl(lang as string, '/')}
            class="text-zink-600 dark:text-zink-300 text-sm uppercase hover:underline"
          >
            {lang}
          </a>
        )}
      </li>
    ))
  }
</ul>
