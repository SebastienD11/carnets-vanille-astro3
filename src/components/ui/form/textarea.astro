---
type Props = {
  name: string
  label: string
  placeholder?: string
  id?: string
  required?: boolean
}

const { id, name, label, required, placeholder }: Props = Astro.props
---

<div>
  <label for={id ? id : null} class="sr-only">{label}</label>
  <textarea
    name={name ? name : null}
    id={id ? id : null}
    rows="4"
    class="peer block w-full appearance-none border-0 border-b-2 border-slate-900 bg-slate-900/5 px-2.5 pb-2.5 pt-5 text-sm text-slate-900 placeholder:text-slate-500 focus:outline-none focus:ring-0 dark:border-slate-600 dark:bg-slate-600/40 dark:text-slate-50 dark:placeholder:text-slate-300"
    placeholder={placeholder ? placeholder : null}
    required={required}></textarea>
</div>
